#include "main.h"
/**
 * mul - multiply two numbers with each other
 * @a: the first number
 * @b: the second number
 * Return: the multiplication value
 */
int mul(int a, int b)
{
	return (a * b);
}
