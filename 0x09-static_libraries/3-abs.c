#include "main.h"
/**
 * _abs - compute the absolute value of a number
 * @n: the integer needed to computed to absolute value
 * Return: the absolute value of the integer
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (n * -1);
}
}
